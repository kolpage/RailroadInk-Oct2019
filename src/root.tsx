import * as React from 'react';
import * as ReactDOM from 'react-dom';

const Index = () => {
    return <div>Welcome to Trains Ink!</div>;
};

ReactDOM.render(<Index />, document.getElementById('app'));