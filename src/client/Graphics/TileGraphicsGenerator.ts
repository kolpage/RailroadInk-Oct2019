import { Orientation, TileType } from "../../common/Enums";

export class TileGraphicsFactory {
    constructor(context: CanvasRenderingContext2D, length: number) {
        
    }

    public GetTileGraphic(tileType: TileType) {
        

    }
}